
## 中英文限制，长度限制
```js
/**
 *
 * @param value 搜索输入值
 * 1, 限制中文，或英文长度未50 （投，被保人）
 * 2，限制只能是英文或数字长度为22，24（保单号）
 */
export const checkSearch = (value) =>
  /(^[A-Za-z]{1,50}$)|(^[\u4e00-\u9fa5]{1,50}$)|(^[a-zA-Z0-9]{22}$)|(^[a-zA-Z0-9]{24}$)/.test(
    value
  );

```